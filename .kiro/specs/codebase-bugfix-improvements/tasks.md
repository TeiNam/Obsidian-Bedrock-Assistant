# 구현 계획

## 버그 수정 및 개선사항 (12개 항목)

- [x] 1. 간단한 수정 일괄 적용 (항목 2, 3, 5, 12)
  - [x] 1.1 clearChat() 첨부 파일 상태 초기화 추가 (`src/chat-view.ts`)
    - `this.attachedFiles.clear()` 추가
    - `this.manuallyAttachedPaths.clear()` 추가
    - `this.autoAttachedPath = null` 추가
    - _Bug_Condition: isBugCondition_ClearChat - clearChat() 호출 시 attachedFiles, manuallyAttachedPaths, autoAttachedPath가 초기화되지 않음_
    - _Expected_Behavior: 모든 첨부 파일 상태가 완전히 리셋됨_
    - _Preservation: clearChat() 기존 동작(메시지 초기화, 환영 메시지, 히스토리 삭제) 유지_
    - _Requirements: 1.2, 2.2, 3.3_
  - [x] 1.2 onClose() 드롭다운 이벤트 리스너 정리 추가 (`src/chat-view.ts`)
    - `this.closeModelDropdown()` 호출 추가
    - _Bug_Condition: isBugCondition_OnClose - onClose() 시 closeModelDropdown() 미호출로 리스너 누수_
    - _Expected_Behavior: document 레벨 이벤트 리스너가 해제됨_
    - _Preservation: 모델 드롭다운 열기/닫기 동작 기존과 동일_
    - _Requirements: 1.3, 2.3, 3.4_
  - [x] 1.3 MCP 타임아웃 후 늦은 응답 디버그 로깅 추가 (`src/mcp-client.ts`)
    - `pending.has(id)` 체크 후 false인 경우 `console.debug()` 로그 추가
    - 로그에 응답 ID와 "타임아웃 후 늦은 응답 도착" 메시지 포함
    - _Bug_Condition: isBugCondition_LateResponse - 타임아웃으로 pending Map에서 제거된 요청에 늦은 응답 도착 시 무시됨_
    - _Expected_Behavior: 디버그 로그 출력으로 추적 가능_
    - _Preservation: 정상 응답의 resolve/reject 처리 기존과 동일_
    - _Requirements: 1.5, 2.5, 3.6_
  - [x] 1.4 AWS SDK 버전 통일 (`package.json`)
    - `@aws-sdk/client-bedrock-runtime` 버전을 `^3.997.0`으로 변경
    - _Bug_Condition: isBugCondition_SdkVersion - client-bedrock과 client-bedrock-runtime 버전 불일치_
    - _Expected_Behavior: 두 패키지 버전이 동일하게 ^3.997.0_
    - _Preservation: Converse, Embedding, ListModels API 호출 정상 동작_
    - _Requirements: 1.12, 2.12, 3.13_

- [x] 2. ToolConfirmModal 연동 구현 (`src/chat-view.ts`)
  - [x] 2.1 파괴적 도구 목록 상수 정의 및 executeAndRenderTool() 수정
    - `DESTRUCTIVE_TOOLS` 상수 정의: `['edit_note', 'create_note', 'delete_file', 'move_file']`
    - `executeAndRenderTool()` 시작 부분에 `confirmToolExecution` 설정 확인 분기 추가
    - 파괴적 도구일 경우 `ToolConfirmModal`을 Promise로 감싸서 사용자 응답 대기
    - 사용자 거부 시 "도구 실행이 취소되었습니다" 메시지 반환 및 UI 업데이트
    - _Bug_Condition: isBugCondition_ToolConfirm - confirmToolExecution=true이고 파괴적 도구 호출 시 모달 미표시_
    - _Expected_Behavior: ToolConfirmModal 표시 후 사용자 확인/취소 선택 가능, 취소 시 실행 중단_
    - _Preservation: confirmToolExecution=false이거나 비파괴적 도구일 때 기존처럼 즉시 실행_
    - _Requirements: 1.1, 2.1, 3.1, 3.2_
  - [x] 2.2 ToolConfirmModal 연동 vitest 테스트 작성
    - **Property 1: Fault Condition** - 파괴적 도구 실행 전 확인 모달 표시
    - `confirmToolExecution=true` + 파괴적 도구 → 모달 표시 확인
    - 모달 승인 시 도구 실행 확인
    - 모달 거부 시 도구 실행 중단 확인
    - **Property 2: Preservation** - 비파괴적 도구 및 설정 비활성화 시 동작 보존
    - `confirmToolExecution=false` → 모든 도구 즉시 실행 확인
    - 비파괴적 도구(`search_vault`, `read_note`, `list_files`) → 설정과 관계없이 즉시 실행 확인
    - _Requirements: 2.1, 3.1, 3.2_

- [x] 3. BedrockClient 코드 중복 제거 리팩토링 (`src/bedrock-client.ts`)
  - [x] 3.1 공통 헬퍼 메서드 추출
    - `private buildClientConfig()` 헬퍼 메서드 추출 (credential 설정 로직: manual/apikey/env 분기)
    - `private applyApiKeyMiddleware(client)` 헬퍼 메서드 추출 (API key 미들웨어 주입)
    - `createClient()`와 `createControlClient()`가 헬퍼를 호출하도록 리팩토링
    - _Bug_Condition: isBugCondition_DuplicateClient - credential 설정 및 미들웨어 로직이 두 메서드에 중복_
    - _Expected_Behavior: 공통 헬퍼를 통해 재사용, 한 곳에서 관리_
    - _Preservation: manual, apikey, env 세 가지 credential 모드가 기존과 동일하게 동작_
    - _Requirements: 1.4, 2.4, 3.5_
  - [x] 3.2 BedrockClient 리팩토링 vitest 테스트 작성
    - **Property 1: Fault Condition** - 공통 헬퍼 메서드를 통한 중복 제거 확인
    - `buildClientConfig()`가 세 가지 credential 모드에서 올바른 설정 반환 확인
    - `createClient()`와 `createControlClient()`가 동일한 헬퍼를 사용하는지 확인
    - **Property 2: Preservation** - 세 가지 credential 모드 동작 보존
    - manual 모드: accessKeyId, secretAccessKey, region 설정 확인
    - apikey 모드: API key 미들웨어 주입 확인
    - env 모드: 환경 변수 기반 credential 확인
    - _Requirements: 2.4, 3.5_

- [x] 4. vault-indexer 진행률 역행 수정 (`src/vault-indexer.ts`)
  - [x] 4.1 indexVault() 진행률 계산 수정
    - 재시도 성공 시 진행률이 단조 증가하도록 수정
    - `onProgress` 콜백에 전달하는 값이 이전 호출보다 항상 크거나 같도록 보정
    - _Bug_Condition: isBugCondition_ProgressRegression - failures.pop() 후 processed++로 진행률 역행 가능_
    - _Expected_Behavior: 진행률 값이 단조 증가(monotonically increasing)_
    - _Preservation: 첫 시도 성공 파일들의 진행률 정상 증가 유지_
    - _Requirements: 1.6, 2.6, 3.7_
  - [x] 4.2 진행률 단조 증가 vitest 테스트 작성
    - **Property 1: Fault Condition** - 재시도 성공 시 진행률 단조 증가
    - 파일 인덱싱 실패 → 재시도 성공 시나리오에서 `onProgress` 값이 항상 이전보다 크거나 같은지 확인
    - **Property 2: Preservation** - 첫 시도 성공 파일 진행률 보존
    - 재시도 없는 정상 인덱싱에서 진행률이 기존처럼 정상 증가하는지 확인
    - _Requirements: 2.6, 3.7_

- [x] 5. handleSend() 원본 메시지 변이 방지 (`src/chat-view.ts`)
  - [x] 5.1 API 호출용 메시지 복사본 생성
    - `lastMsg.content = contextPrefix + text` 대신 API 호출용 메시지만 컨텍스트 접두사 포함
    - `this.messages` 배열의 원본 `content`는 변경하지 않음
    - `generateResponse()` 호출 시 컨텍스트 접두사를 별도 파라미터로 전달하거나, 내부에서 복사본 생성
    - _Bug_Condition: isBugCondition_MessageMutation - 첨부 파일 컨텍스트가 있을 때 원본 메시지 content를 직접 변경_
    - _Expected_Behavior: 원본 메시지 불변, API 호출용 복사본만 컨텍스트 포함_
    - _Preservation: 첨부 파일 없는 일반 메시지 전송/저장 정상 동작_
    - _Requirements: 1.7, 2.7, 3.8_
  - [x] 5.2 메시지 불변성 vitest 테스트 작성
    - **Property 1: Fault Condition** - 첨부 파일 컨텍스트 시 원본 메시지 보존
    - 첨부 파일 있는 메시지 전송 후 `this.messages` 배열의 원본 content가 변경되지 않았는지 확인
    - **Property 2: Preservation** - 첨부 파일 없는 메시지 동작 보존
    - 일반 메시지 전송 시 기존과 동일하게 동작하는지 확인
    - _Requirements: 2.7, 3.8_

- [x] 6. estimateTokens() 토큰 추정 통일 (`src/token-trimmer.ts`)
  - [x] 6.1 토큰 추정 비율 상수화 및 통일
    - `CHARS_PER_TOKEN = 2.5` 상수 정의
    - `estimateTokens()`에서 `/ 4`를 `/ CHARS_PER_TOKEN`으로 변경
    - `updateContextRing()`에서도 동일 상수 사용 확인
    - _Bug_Condition: isBugCondition_TokenEstimate - estimateTokens()는 4자/토큰, updateContextRing()은 2.5자/토큰 사용_
    - _Expected_Behavior: 두 함수 모두 일관되게 2.5자/토큰 사용_
    - _Preservation: trimConversationHistory() 컨텍스트 윈도우 초과 방지 및 최소 메시지 유지 로직 정상 동작_
    - _Requirements: 1.8, 2.8, 3.9_
  - [x] 6.2 토큰 추정 일관성 vitest 테스트 작성
    - **Property 1: Fault Condition** - 토큰 추정 비율 일관성
    - `estimateTokens()`와 `updateContextRing()`이 동일한 비율을 사용하는지 확인
    - 동일 텍스트에 대해 두 함수의 토큰 추정 결과가 일치하는지 확인
    - **Property 2: Preservation** - trimConversationHistory() 동작 보존
    - 토큰 추정 변경 후에도 컨텍스트 윈도우 초과 방지 로직이 정상 동작하는지 확인
    - _Requirements: 2.8, 3.9_

- [x] 7. session-recovery.ts 모듈 활용 리팩토링 (`src/main.ts`)
  - [x] 7.1 main.ts에서 session-recovery.ts 함수 활용
    - `session-recovery.ts`에서 `loadSessionsWithRecovery`, `saveSessionsWithBackup` import
    - `loadSessions()`와 `saveSessions()`가 해당 함수를 호출하도록 리팩토링
    - 인라인 중복 코드 제거
    - _Bug_Condition: isBugCondition_SessionRecovery - main.ts에서 session-recovery.ts 함수를 사용하지 않고 인라인 구현_
    - _Expected_Behavior: session-recovery.ts 모듈 재사용으로 중복 제거_
    - _Preservation: 기존 세션 파일 호환성 유지, 백업/복구 동작 동일_
    - _Requirements: 1.9, 2.9, 3.10_

- [x] 8. createNote() 부모 폴더 자동 생성 (`src/obsidian-tools.ts`)
  - [x] 8.1 createNote()에 부모 폴더 확인/생성 로직 추가
    - `applyTemplate()`, `moveFile()`과 동일한 패턴 적용
    - 경로에서 디렉토리 부분 추출 후 폴더 존재 여부 확인
    - 폴더가 없으면 `vault.createFolder()` 호출
    - _Bug_Condition: isBugCondition_CreateNoteFolder - 중첩 경로에 노트 생성 시 부모 폴더 미생성_
    - _Expected_Behavior: 부모 폴더가 없으면 자동 생성_
    - _Preservation: 루트 레벨 createNote() 시 폴더 생성 없이 바로 노트 생성_
    - _Requirements: 1.10, 2.10, 3.11_
  - [x] 8.2 createNote() 폴더 자동 생성 vitest 테스트 작성
    - **Property 1: Fault Condition** - 중첩 경로 노트 생성 시 폴더 자동 생성
    - `createNote("folder/sub/note.md", "...")` 호출 시 부모 폴더가 자동 생성되는지 확인
    - **Property 2: Preservation** - 루트 레벨 노트 생성 동작 보존
    - `createNote("note.md", "...")` 호출 시 폴더 생성 없이 바로 노트가 생성되는지 확인
    - _Requirements: 2.10, 3.11_

- [x] 9. addFileContext() 파일 확장자 제한 완화 (`src/chat-view.ts`)
  - [x] 9.1 허용 확장자 목록 확장
    - `file.extension !== "md"` 조건을 허용 목록 기반으로 변경
    - `ALLOWED_TEXT_EXTENSIONS` 상수 정의: `['md', 'txt', 'json', 'yaml', 'yml', 'csv', 'xml', 'html', 'css', 'js', 'ts']`
    - _Bug_Condition: isBugCondition_FileExtension - .md 외 텍스트 파일이 거부됨_
    - _Expected_Behavior: 텍스트 기반 파일 확장자 허용_
    - _Preservation: .md 파일 첨부가 기존처럼 정상 동작_
    - _Requirements: 1.11, 2.11, 3.12_
  - [x] 9.2 파일 확장자 필터링 vitest 테스트 작성
    - **Property 1: Fault Condition** - 텍스트 파일 확장자 허용
    - `.txt`, `.json`, `.yaml`, `.js`, `.ts` 등 텍스트 파일이 허용되는지 확인
    - **Property 2: Preservation** - .md 파일 첨부 동작 보존
    - `.md` 파일이 기존처럼 정상 첨부되는지 확인
    - 바이너리 파일(`.png`, `.jpg`, `.exe` 등)이 여전히 거부되는지 확인
    - _Requirements: 2.11, 3.12_

- [x] 10. 체크포인트 - 전체 테스트 통과 확인
  - 모든 vitest 테스트가 통과하는지 확인
  - 기존 테스트가 깨지지 않았는지 확인
  - 문제 발생 시 사용자에게 확인 요청
